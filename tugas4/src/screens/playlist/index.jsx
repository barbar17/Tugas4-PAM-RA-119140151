import React from "react";
import { View, Text } from "react-native";

const PlayList = () => {
    return (
        <View>
            <Text>HAI FROM PLAYLIST</Text>
        </View>
    );
};

export default PlayList;