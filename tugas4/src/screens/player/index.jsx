import React from "react";
import { View, Text } from "react-native";

const Player = () => {
    return (
        <View>
            <Text>HAI FROM PLAYER</Text>
        </View>
    );
};

export default Player;